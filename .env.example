# NEFOS Project Environment Variables
# Copy this file to .env and update values as needed
# Note: Some values differ between Docker and host execution contexts

# ============================================
# PostgreSQL Database Configuration
# ============================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123
POSTGRES_MULTIPLE_DATABASES=userdb,teamdb

# Database connection settings for services
DB_USER=postgres
DB_PASSWORD=postgres123
# IMPORTANT: Use 'postgres' for Docker containers, 'localhost' for host scripts
DB_HOST=postgres
DB_PORT=5432

# Individual database names
USER_DB_NAME=userdb
TEAM_DB_NAME=teamdb

# ============================================
# MongoDB Configuration
# ============================================
# IMPORTANT: Use 'mongodb' for Docker containers, 'localhost' for host scripts
MONGO_HOST=mongodb
MONGO_PORT=27017
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=mongodb123
MONGO_DATABASE=taskdb
MONGO_AUTH_DATABASE=admin

# ============================================
# JWT Authentication
# ============================================
# SECURITY: Generate a secure key for production using:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your_very_long_and_secure_jwt_secret_key_change_this_in_production

# ============================================
# Setup Script - Superuser Credentials
# ============================================
# UserService Admin (uses email for login)
SETUP_USER_EMAIL=admin@example.com
SETUP_USER_PASSWORD=admin123
SETUP_USER_FIRST_NAME=Admin
SETUP_USER_LAST_NAME=User

# TeamService Admin (uses username for login)
SETUP_TEAM_USERNAME=admin
SETUP_TEAM_PASSWORD=admin123
SETUP_TEAM_EMAIL=admin@example.com

# ============================================
# Frontend Configuration
# ============================================
NODE_ENV=development

# ============================================
# Important Notes
# ============================================
# 1. DB_HOST and MONGO_HOST:
#    - Use 'postgres' and 'mongodb' for Docker containers (default)
#    - The setup.sh script automatically changes these to 'localhost' 
#      when running from the host machine
#
# 2. Security:
#    - Change all passwords in production!
#    - Generate a new JWT_SECRET_KEY using:
#      python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# 3. Seeded Data:
#    - The setup creates the admin user above plus:
#      - 2 team leaders (leader1@example.com, leader2@example.com)
#      - 2 members (member1@example.com, member2@example.com)
#      - All seeded users have password: password123
#      - 2 teams with assigned members
#      - 4 sample tasks
